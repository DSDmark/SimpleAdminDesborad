import {
  Link as MuiLink,
  AppBar,
  Grid,
  Box,
  Toolbar,
  Typography,
} from '@mui/material'

const NavBar = () => {
  return (
    <AppBar position='static'>
      <Toolbar>dev you</Toolbar>
    </AppBar>
  )
}

export default NavBar
